export interface FloorResponseDTO {
  id: string;
  buildingId:string;
  width: number;
  length: number;
  floorNumber: number;
  description: string;
  floorMap: number[][];
}

export interface FloorDTO {
  id: string;
  buildingId:string;
  width: number;
  length: number;
  floorNumber: number;
  description: string;
  floorMap: number[][];
}

