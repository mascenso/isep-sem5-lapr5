export default interface IBridgeDTO {
        id: string;
        code: string;
        name: string;
        floorAId: string;
        floorBId: string;
        buildingAId: string;
        buildingBId: string;
}
